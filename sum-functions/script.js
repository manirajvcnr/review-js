// Sum of two numbers using functions

let a=parseInt(prompt('enter the first number')),
b=parseInt(prompt('enter the first number'));

function add(p1,p2){
	console.log("The sum of two number is "+(p1+p2));
}
add(a,b);